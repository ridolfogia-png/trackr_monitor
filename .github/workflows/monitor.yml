name: Trackr Summer UK Monitor

on:
  schedule:
    # Every 30 minutes. 15-min was too tight: runs occasionally took ~15 min
    # (browser install + slow page load), overlapped, and the concurrency rule
    # cancelled the queued run -> "Run failed / cancelled" emails. 30 min gives
    # plenty of headroom and is more than enough for internship openings.
    - cron: "*/30 * * * *"
  workflow_dispatch: {}   # adds a manual "Run workflow" button

permissions:
  contents: write          # needed to commit the updated state back

concurrency:
  group: trackr-monitor
  cancel-in-progress: false

jobs:
  check:
    runs-on: ubuntu-latest
    timeout-minutes: 12        # if a run ever hangs, kill it cleanly instead of lingering
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: playwright-chromium-1.48.0

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          python -m playwright install --with-deps chromium

      - name: Run monitor
        env:
          SMTP_USER: ${{ secrets.SMTP_USER }}
          SMTP_PASS: ${{ secrets.SMTP_PASS }}
          MAIL_TO:   ${{ secrets.MAIL_TO }}
          SMTP_HOST: ${{ secrets.SMTP_HOST }}   # optional, defaults to smtp.gmail.com
          SMTP_PORT: ${{ secrets.SMTP_PORT }}   # optional, defaults to 587
        run: python monitor.py

      - name: Persist state
        run: |
          if [[ -n "$(git status --porcelain state)" ]]; then
            git config user.name  "trackr-monitor"
            git config user.email "actions@users.noreply.github.com"
            git add state
            git commit -m "state: update $(date -u +%FT%TZ)"
            git push
          else
            echo "No state changes."
          fi
